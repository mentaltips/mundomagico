# Necessário para Next.js + pnpm monorepo no Vercel
# O next-font-loader e o CSS pipeline do Next.js precisam
# encontrar tailwindcss/postcss/autoprefixer na raiz do node_modules.
# shamefully-hoist resolve isso hoistando todos os pacotes.
shamefully-hoist=true
